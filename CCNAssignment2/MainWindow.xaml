<Window x:Class="CCNAssignment2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CCNAssignment2"
        mc:Ignorable="d"
        Title="Library Management System" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Library Management System" 
                 FontSize="24" 
                 HorizontalAlignment="Center" 
                 Margin="0,20,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Loan Operations -->
            <GroupBox Grid.Column="0" Header="Loan Operations" Margin="10">
                <StackPanel Margin="10">
                    <Button x:Name="BorrowBookButton" Content="Borrow Book" Margin="0,5" Padding="5"/>
                    <Button x:Name="ReturnBookButton" Content="Return Book" Margin="0,5" Padding="5"/>
                    <Button x:Name="RenewLoanButton" Content="Renew Loan" Margin="0,5" Padding="5"/>
                </StackPanel>
            </GroupBox>

            <!-- View Operations -->
            <GroupBox Grid.Column="1" Header="View Operations" Margin="10">
                <StackPanel Margin="10">
                    <Button x:Name="ViewAllBooksButton" Content="View All Books" Margin="0,5" Padding="5"/>
                    <Button x:Name="ViewAllMembersButton" Content="View All Members" Margin="0,5" Padding="5"/>
                    <Button x:Name="ViewCurrentLoansButton" Content="View Current Loans" Margin="0,5" Padding="5"/>
                </StackPanel>
            </GroupBox>

            <!-- Results Area -->
            <GroupBox Grid.Column="2" Header="Results" Margin="10">
                <ScrollViewer Margin="10">
                    <TextBlock x:Name="ResultsTextBlock" 
                             TextWrapping="Wrap"/>
                </ScrollViewer>
            </GroupBox>
        </Grid>
    </Grid>
</Window>
